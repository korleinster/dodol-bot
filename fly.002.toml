app = 'dodol-bot-002'
primary_region = 'nrt'

[build]

[env]
  BOT_NUMBER = '2'

[[mounts]]
  source = 'dodol_bot_002_data'
  destination = '/app/data'
  initial_size = '1gb'

[vm]
  cpus = 1
  cpu_kind = 'shared'
  memory = '512mb'
