# 테스트 명세

`test_bot.py` — 별도 테스터 봇을 통한 E2E 기능 테스트.

---

## 실행 방법

```bash
# .env에 추가 필요
TESTER_TOKEN=테스터봇_토큰
TEST_CHANNEL_ID=테스트할_채널_ID

python test_bot.py
```

- 도돌봇이 이미 배치된 채널에서 실행해야 함.
- 테스터 봇이 명령을 전송하고, 도돌봇의 응답을 수신해 검증.
- 각 테스트 사이 1초 대기.

---

## 테스트 항목 (17개)

| # | 테스트명 | 입력 명령 | 검증 키워드 | 비고 |
|---|---|---|---|---|
| 1 | 도움말 (메뉴) | `메뉴` | `도돌봇` | embed footer 기준 |
| 2 | 보스 등록 | `보스등록 0300 테스트보스(ㅌㅅ)` | `등록 완료`, `테스트보스` | 커스텀 보스 신규 등록 |
| 3 | 보스 목록 | `보스` | `테스트보스` | 방금 등록한 보스 포함 여부 |
| 4 | 컷 처리 | `테스트보스 컷` | `컷 처리`, `다음 리스폰` | embed 필드 확인 |
| 5 | 초성 컷 | `ㅌㅅ ㅋ` | `컷 처리` | 초성 매칭 후 컷 처리 |
| 6 | 멍 처리 | `테스트보스 멍` | `멍 처리` | |
| 7 | 초성 멍 | `ㅌㅅ ㅁ` | `멍 처리` | 초성 매칭 후 멍 처리 |
| 8 | 보탐 | `보탐` | `테스트보스` | 예약 목록에 포함 여부 |
| 9 | 임의 예약 | `23:59 테스트알림` | `테스트알림`, `예약 완료` | 임의 시각 예약 |
| 10 | 예약 삭제 | `테스트알림 삭제` | `삭제` | ⚠️ 현재 삭제 기능 제거됨 → FAIL 예상 |
| 11 | 전체 초기화 | `초기화` | `초기화` | 고정 제외 전체 삭제 |
| 12 | 날씨 | `날씨` | `날씨 예보` | Open-Meteo API, timeout 10초 |
| 13 | 시세 검색 | `시세 싸울아비 장검` | `검색` | PLAYNC API, timeout 10초 |
| 14 | 주사위 | `주사위` | `🎲` | |
| 15 | 동전 | `동전` | `앞면` 또는 `뒷면` | 둘 중 하나만 포함되면 PASS |
| 16 | 경마 | `경마` | `경마` | timeout 20초 (애니메이션 대기) |
| 17 | 보스 삭제 (정리) | `보스삭제 테스트보스` | `삭제 완료` | 테스트 후 등록한 보스 정리 |

---

## 검증 방식

- 응답 메시지의 `content`, embed `title`, `description`, `field name/value`를 모두 합산한 문자열에서 키워드 검색.
- 응답 없음 (5초 timeout): FAIL — "응답 없음 (timeout)"
- 키워드 미포함: FAIL — `'{keyword}' 없음`

---

## 현재 알려진 FAIL 항목

| # | 항목 | 원인 |
|---|---|---|
| 10 | 예약 삭제 | 예약 삭제 명령이 boss.py에서 제거됨 (기능 폐기) |
| 2 | 보스 등록 | `보스등록` 명령이 boss.py에 미구현 (커스텀 보스 등록 기능 없음) |

> 위 2개 항목은 test_bot.py 업데이트가 필요함.

---

## 결과 출력 형식

```
✅ PASS 도움말 (메뉴)
❌ FAIL 예약 삭제 — '삭제' 없음
⏭️  SKIP (미사용)

==================================================
  결과: 15/17 통과  |  실패: 2
==================================================
```
