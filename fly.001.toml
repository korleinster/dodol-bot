app = 'dodol-bot-001'
primary_region = 'nrt'

[build]

[env]
  BOT_NUMBER = '1'

[[mounts]]
  source = 'dodol_bot_001_data'
  destination = '/app/data'
  initial_size = '1gb'

[vm]
  cpus = 1
  cpu_kind = 'shared'
  memory = '256mb'
